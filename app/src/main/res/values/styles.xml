<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/sans_regular</item>
    </style>

    <style name="TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">@dimen/default_title_size</item>
    </style>

    <style name="DialogError" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColorPrimary">@color/colorError</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="editTextBackground" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_edittext</item>
    </style>

    <style name="FacebookLoginButton">
        <item name="android:background">@drawable/facebook_button_bg</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_height">102dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

</resources>
